---
description: Append a new collaboration journal entry to the current module's JOURNAL.md using the course's standard template.
---

Figure out which module this session is working in (ask if it's not obvious from context or recent file activity). Then add a new entry to `modules/assisted-to-agentic-module-N/JOURNAL.md` (create it with a top-level `# Module N Journal` heading if it doesn't exist yet) using this template:

- Prompt:
- Tool:
- Mode:
- Context:
- Model:
- Input:
- Output:
- Cost:
- Reflections:

Fill in whatever the user has already told you in: $ARGUMENTS

Ask for anything essential that's still missing (at minimum Prompt, Tool, and Output) rather than guessing. Append the entry to the end of the file, in chronological order.
